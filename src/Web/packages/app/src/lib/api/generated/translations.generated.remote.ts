// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { TranslationContributionRequestSchema, UpsertTranslationDraftsRequestSchema, SubmitTranslationDraftsRequestSchema } from '$lib/api/generated/schemas';
import { type TranslationContributionRequest, type UpsertTranslationDraftsRequest, type SubmitTranslationDraftsRequest } from '$api';

export const submitContribution = command(TranslationContributionRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.translations.submitContribution(request as TranslationContributionRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in translations.submitContribution:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to submit contribution');
  }
});

export const getDrafts = query(z.object({ locale: z.string().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.translations.getDrafts(params?.locale);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { request, url } = getRequestEvent();
    const shareHost = request.headers.get('x-forwarded-host') ?? request.headers.get('host') ?? '';
    if (/^[^.]+\.share\./i.test(shareHost)) throw error(401, 'Unauthorized');
    throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in translations.getDrafts:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to get drafts');
  }
});

export const upsertDrafts = command(UpsertTranslationDraftsRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.translations.upsertDrafts(request as UpsertTranslationDraftsRequest);
    await Promise.all([
      getDrafts(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in translations.upsertDrafts:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to upsert drafts');
  }
});

export const clearDrafts = command(z.object({ locale: z.string().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.translations.clearDrafts(params?.locale);
    await Promise.all([
      getDrafts(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in translations.clearDrafts:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to clear drafts');
  }
});

export const submitDrafts = command(SubmitTranslationDraftsRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.translations.submitDrafts(request as SubmitTranslationDraftsRequest);
    await Promise.all([
      getDrafts(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in translations.submitDrafts:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to submit drafts');
  }
});
