// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { SleepSource } from '$api';

/** Get the full single-night report for a sleep session, including stage breakdown,
overnight TIR, hypo events, dawn phenomenon, and wake events. */
export const getSingleNight = query(z.string(), async (sessionId) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.sleepReport.getSingleNight(sessionId);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in sleepReport.getSingleNight:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([k, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to get single night');
  }
});

/** Get a multi-night trends report. Maximum date range is 90 days.
When source is omitted, sessions are deduplicated to one per
calendar night (longest sleep wins; source priority as tie-breaker). */
export const getTrends = query(z.object({ from: z.coerce.date().optional(), to: z.coerce.date().optional(), source: z.enum(SleepSource).optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.sleepReport.getTrends(params?.from, params?.to, params?.source);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in sleepReport.getTrends:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([k, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to get trends');
  }
});
